.xepan-grid.main-box.clearfix.atk-table.atk-table-zebra.atk-table-bordered(id="{$_name}" class="{$class}")
    {Pannel}
    header.main-box-header.clearfix.atk-actions(class=" {$class} ")
        div.filter-block( style="100%")
            .pull-right {$grid_buttons} 
            .form-group.pull-right.xepan-filter-form
                span(class="form-control pull-left")|{$quick_search}
                    i(class="fa fa-search search-icon")
    {/Pannel}
    .main-box-body.clearfix.table-responsive
        table.table.user-list.table-hover.footable.toggle-circle-filled(style="width:100%")
            {header}
            thead.xepan-hover-hand
                {cols}{col}
                th(id="{$name_sortid}")
                    span(style= "cursor:pointer; cursor: hand; max-width:600px;") Warehouse {$name_sort} 
                th(id="" data-hide="all")
                    span(style= "cursor:pointer; cursor: hand; max-width:600px;") ADDRESS   
                th(id="" data-hide="phone")
                    span(style= "cursor:pointer; cursor: hand; max-width:600px;") Organization  
                th(id="" data-hide="phone")
                    span(style= "cursor:pointer; cursor: hand; max-width:600px;") Country  
                th(id="" data-hide="phone")
                    span(style= "cursor:pointer; cursor: hand; max-width:600px;") State  
                th(id="" data-hide="phone")
                    span(style= "cursor:pointer; cursor: hand; max-width:600px;") City  
                th(id="" data-hide="all")
                    span(style= "cursor:pointer; cursor: hand; max-width:600px;") PIN CODE   
                
                th 
                {/}{/}
            {/}
            {$subheader}

            tbody
                {rows}{row}
                tr(class="{$odd_even}" data-id="{$id}")
                    {cols}
                    td
                      a {$first_name}&nbsp;{$last_name}
                    td
                      |{$address}
                    td
                      | {$organization}
                    td
                      | {$country}
                    td
                      | {$state}
                    td
                      | {$city}
                    td
                      | {$pin_code}
                    td(style="width: 15%;")
                        {$edit}
                        {$delete}
                    
                    {/}
                {/}{/}
                {not_found}
                div.row
                    .col-md-12.text-center.alert.alert-warning(role="alert")
                        span.icon-attention.fa.fa-exclamation-triangle  &nbsp;{$not_found_message}
                {/}

            {totals}
            tfoot
                tr(class="totals")
                    {cols}{col}
                    <td {$tdparam}>
                    {$content}
                    </td>
                    {/}{/}
            {/}
        {$Paginator}
