.xepan-grid.main-box.clearfix.atk-table.atk-table-zebra.atk-table-bordered(id="{$_name}" class="{$class}")
    {Pannel}
    header.main-box-header.clearfix.atk-actions(class=" {$class} ")
        div.filter-block( style="100%")
            .pull-right {$grid_buttons} 
            .form-group.pull-right.xepan-filter-form
                span(class="form-control pull-left")|{$quick_search}
                    i(class="fa fa-search search-icon")
    {/Pannel}
    .main-box-body.clearfix.table.table-responsive
            table.table.user-list.table-hover
                {header}
                thead.xepan-hover-hand
                    {cols}{col}
                    th
                    th(id="{$name_sortid}")
                        span Name 
                        span(style= "cursor:pointer; cursor: hand; max-width:600px;"){$name_sort}
                    th(id="{$display_type_sortid}")
                        span Display Type 
                        span(style= "cursor:pointer; cursor: hand;"){$display_type_sort} 
                    th(id="{$sequence_order_sortid}")
                        span Sequence Order
                        span(style= "cursor:pointer; cursor: hand;"){$sequence_order_sort}
                    th Is Filterable
                    th Value
                    th
                    {/}{/}
                {/}
                tbody
                    {rows}{row}
                    tr(class="{$odd_even}" data-id="{$id}")
                        {cols}
                        td {$selected}
                        td {$name}
                        td.atk-form-field.atk-form-row {$display_type}
                        td {$sequence_order}
                        td {$is_filterable}
                        td {$value}
                        td(style="width: 15%;")
                            {$edit}
                            {$delete}
                        
                        {/}
                    {/}{/}
                    {not_found}
                    div.row
                        .col-md-12.text-center.alert.alert-warning(role="alert")
                            span.icon-attention.fa.fa-exclamation-triangle  &nbsp;{$not_found_message}
                    {/}

                {totals}
                tfoot
                    tr(class="totals")
                        {cols}{col}
                        <td {$tdparam}>
                        {$content}
                        </td>
                        {/}{/}
                {/}
            {$Paginator}
